{"id":"mem_acac","domain":"repository","type":"pattern","title":"control/ is a deterministic state machine; no LLM call belongs there","body":"The control plane forks VMs, dispatches agents, and reconciles run state over Postgres/SQLite rows. All intelligence lives inside the forked VM, in the agent. control/README.md states this explicitly as an architectural invariant: 'If a model call ever appears in this repo, something has gone wrong architecturally.' When extending control/, prefer state-machine transitions on the `runs` table over adding judgment calls.","resolution":null,"evidence":{"files":["control/README.md","control/runner.py"],"dirs":["control"],"branch":"factory/issue-47","issues":["mithril-studio/software-factory#47"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-19T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id":"mem_5b24","domain":"repository","type":"convention","title":"control and telemetry share one SQLite file but own separate tables and connection helpers","body":"control/db.py and telemetry/store.py both point at the same `var/factory.db` (control/config.py:166, telemetry/config.py:20), but each defines its own connect()/init() over aiosqlite and its own tables, with idempotent init() called on boot so there is no separate migration step. telemetry never imports control.db, and control never reads a telemetry table (telemetry/store.py's module docstring); the two communicate only through that shared database file and run_id. Dependency direction is one-way — control imports telemetry, never the reverse (telemetry/README.md \"Dependency direction is one-way\").","resolution":null,"evidence":{"files":["control/db.py","telemetry/store.py","control/config.py","telemetry/config.py","telemetry/README.md"],"dirs":[],"branch":"factory/issue-47","issues":["mithril-studio/software-factory#47"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-19T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id":"mem_b1e0","domain":"repository","type":"pattern","title":"telemetry/normalize.py isolates all runtime-specific event vocabulary behind an adapter","body":"ClaudeCodeAdapter (and the ADAPTERS registry) in telemetry/normalize.py is the one place that knows what a given agent runtime's events look like; no table in the repo mentions Claude by name. Adding a second coding agent means writing a new adapter against events recorded from a real run, not changing callers. An unrecognized `events` string in the golden manifest falls back to NullAdapter (empty results, no exception) rather than failing the run.","resolution":null,"evidence":{"files":["telemetry/normalize.py"],"dirs":[],"branch":"factory/issue-47","issues":["mithril-studio/software-factory#47"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-19T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id":"mem_783b","domain":"repository","type":"convention","title":"Fast verification is ruff --select F,E9 plus plain __main__ test modules, nothing else","body":"CI's only two gates: `.venv/bin/ruff check --select F,E9 control telemetry` (full `ruff check` is red on arrival by design and is not the gate — it reports pre-existing style debt out of scope for any single issue), and running every `control/*_test.py` / `telemetry/*_test.py` as `python -m <module>`. There is no pytest and no test runner; a test registers itself by existing at that path with a `__main__` block, following the shape of the file beside it (verified: both commands pass clean on main@68923d9).","resolution":null,"evidence":{"files":["control/agents_test.py","telemetry/normalize_test.py","pyproject.toml"],"dirs":["control","telemetry"],"branch":"factory/issue-47","issues":["mithril-studio/software-factory#47"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-19T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id":"mem_d79c","domain":"repository","type":"decision","title":"Control plane runs on a Hetzner VM under systemd, not on boxd itself","body":"The control plane moved off boxd on 2026-08-19 after the box it ran on had its root filesystem corrupt, taking run history with it: the thing the factory forks VMs *from* should not also be the thing that remembers what it did. Deploy is `scripts/deploy.sh` (pulls tracked branch, rebuilds, restarts `factory.service`); TLS is Caddy, not boxd's proxy; secrets live only in `.env` on the box. boxd is still where every run VM comes from — the Hetzner box only talks to it over the API (BOXD_API_KEY).","resolution":null,"evidence":{"files":["README.md","scripts/deploy.sh"],"dirs":[],"branch":"factory/issue-47","issues":["mithril-studio/software-factory#47"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-19T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id":"mem_c17a","domain":"repository","type":"convention","title":"One GitHub credential serves the control plane and every run VM, and it needs repo + workflow","body":"control/config.py resolves GITHUB_TOKEN (env first, then `gh auth token`) and runner.dispatch_env hands that same value to every run VM as GH_TOKEN. So one credential covers both the control plane's API calls — labels, run comments, the auto-merge — and everything the agent does inside the VM: clone, push, `gh pr create`. It has to carry `repo` and `workflow`. Without `workflow`, GitHub rejects the whole push the moment any commit in it touches .github/workflows/*, even when every other file in the push is fine; that is what mem_0d3c recorded, and the credential in use since 2026-08-20 has the scope, so workflow files are editable again. Without write access to contents it is worse: the run clones, spends its whole context, and dies at the push — or opens a pull request the auto-merge then cannot land, refused with a bare 403.","resolution":null,"evidence":{"files":["control/config.py","control/runner.py","control/github.py","control/preflight.py"],"dirs":[],"branch":"fix/credential-gates","issues":[],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-20T00:00:00Z"},"status":"active","supersedes":"mem_0d3c","confidence":"high","hits":0}
{"id":"mem_5f2b","domain":"repository","type":"pattern","title":"Goldens are named for the repo, not the agent; the base is golden-copy","body":"agents.resolve_snapshot (control/agents.py) picks `golden-<owner-repo>` (the warm tier, repo pre-cloned) if it exists, else `golden-copy` (the base image: tooling and agent auth, no repo), else nothing. The agent is deliberately absent from the name — which agent an image launches is what the image announces in /etc/factory/agent.json, read off the FACTORY-MANIFEST line and written to runs.agent in control/runner.py. Nothing derives an agent from a snapshot name any more, and there is no FACTORY_AGENT_DEFAULT. The two tiers matter because the fallback is what makes connecting a repo cheap: an unprovisioned repo is not an error, it boots the base and installs for itself — step 2 of the run prompt is \"Install this project's dependencies, before you touch any code\", and the prompt says plainly that \"nothing is installed for this repo yet\". scripts/refresh-golden.sh says it from the other side: \"A warm golden is a speed-up; every run installs for itself\". So a missing warm snapshot costs minutes, not correctness, and provisioning may finish after the first run has already gone out.","resolution":null,"evidence":{"files":["control/agents.py","control/runner.py","control/preflight.py","scripts/refresh-golden.sh"],"dirs":[],"branch":"phase-b/naming-contract","issues":[],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-20T00:00:00Z"},"status":"active","supersedes":"mem_40b7","confidence":"high","hits":0}
{"id":"mem_9e41","domain":"repository","type":"convention","title":"Watched repos are a DB register with a sync cache; nothing about a golden gates dispatch","body":"Repos live in the `repos` table and are connected through `POST /api/repos`; FACTORY_REPOS only seeds it at boot (control/config.py `_repos`, control/repos.py `seed`) and removing an entry never unwatches anything. `control/repos.py` holds an in-memory cache because `settings.repos` was read synchronously by the poller, the plan page, the projects page and preflight — every write goes through that module and reloads, so nothing may write `db.add_repo`/`remove_repo`/`set_repo_golden` directly. The poller starts unconditionally and re-reads `repos.watched()` each tick, so a repo connected at 10:00 is polled at 10:00:30 with no restart. Provisioning a warm golden is a run with `kind = 'provision'` (control/provision.py) and its `repos.provision_status` is a report, never a gate: an unprovisioned, failed or half-warmed repo still dispatches onto golden-copy and installs for itself. Never make connecting or dispatching wait on a snapshot.","resolution":null,"evidence":{"files":["control/repos.py","control/provision.py","control/poller.py","control/app.py","control/db.py"],"dirs":[],"branch":"phase-b/wrapup","issues":[],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-20T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id":"mem_37db","domain":"repository","type":"convention","title":"telemetry/config.py's db_path is a plain module attribute, not a frozen settings object","body":"Unlike control/config.py's `settings` (frozen, so tests use `object.__setattr__`), telemetry/config.py just exposes `db_path` as a bare module-level `Path`. A test pointing telemetry at a throwaway SQLite file assigns `config.db_path = ...` directly before calling `store.init()` — `store.connect()` re-reads `config.db_path` on every call rather than binding it at import, so the reassignment is picked up without any patching helper (control/repos_test.py's pattern is close but heavier than telemetry needs).","resolution":null,"evidence":{"files":["telemetry/config.py","telemetry/store.py","telemetry/store_test.py"],"dirs":["telemetry"],"branch":"factory/issue-49","issues":["mithril-studio/software-factory#49"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-20T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
{"id": "mem_6a59", "domain": "repository", "type": "pattern", "title": "Priming receipts are one FACTORY_MEMORY log line, parsed permissively and never persisted", "body": "control/runner.py defines MEMORY_RECEIPT_MARKER + parse_memory_receipt: the build prompt requires one `FACTORY_MEMORY {\"indexed\":n,\"opened\":[...],\"domains\":[...]}` line right after memory priming (an explicit EMPTY_MEMORY_RECEIPT when .mem/ is absent), and the parser scans free-form run output for the marker, tolerating anything before or after it. Malformed JSON, wrong field types, or no marker at all -> None, never a raise. This module only builds the prompt and parses the line; it does not write telemetry.memory_reads (that storage seam already exists in telemetry/store.py from #49 and is wired up by a later factory-memory-foundation step) and does not judge which memories were useful.", "resolution": null, "evidence": {"files": ["control/runner.py", "control/prompt_profile_test.py", "control/memory_receipt_test.py"], "dirs": [], "branch": "factory/issue-50", "issues": ["mithril-studio/software-factory#50"], "run": null}, "provenance": {"author": "agent:claude-code", "backend": "boxd", "created_at": "2026-08-20T00:00:00Z"}, "status": "active", "supersedes": null, "confidence": "high", "hits": 0}
{"id":"mem_f402","domain":"repository","type":"pattern","title":"Admission-control tables (e.g. memory_candidates) live in control/db.py as INSERT OR IGNORE plus an explicit transition-edge dict","body":"For issue #52 (memory_candidates), the queue followed control/db.py's existing repos-table shape: idempotent insertion via `INSERT OR IGNORE` on the primary key (fields dict includes id, so a resubmission is silently dropped and the first insert's fields win), and status transitions gated by a plain `dict[str, tuple[str, ...]]` of allowed edges (e.g. {\"pending\": (\"accepted\", \"rejected\")}) rather than a state machine library. transition_candidate() raises ValueError on any disallowed edge (including repeating a terminal transition) rather than returning False, so callers can't mistake \"no-op\" for \"just happened\". Tests follow control/repos_test.py's shape: point settings.db_path at a tempfile.TemporaryDirectory() via object.__setattr__ (settings is frozen), then asyncio.run(db.init()) once at module scope.","resolution":null,"evidence":{"files":["control/db.py","control/memory_candidates_test.py"],"dirs":[],"branch":"factory/issue-52","issues":["mithril-studio/software-factory#52"],"run":null},"provenance":{"author":"agent:claude-code","backend":"boxd","created_at":"2026-08-20T00:00:00Z"},"status":"active","supersedes":null,"confidence":"high","hits":0}
